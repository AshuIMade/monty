#include "monty.h"
/**
 * checkdigit - checks if data is an integer
 * @tokens: a token
 * @line_cnt: the number of lines
 * Return: 0 if true, 1 if was converted
 */
int checkdigit(char *tokens, unsigned int line_cnt)
{
	(void)line_cnt;
	(void)tokens;
	return (0);
}
